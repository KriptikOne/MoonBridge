# Approved Batches

Manually cleansed material lives here in named batch subfolders.

**This folder itself is NOT approved for scanning.**

To inventory a batch:
1. Create `YourBatchName/` inside this folder
2. Add the **exact full path** to `04_Lists_And_Records/approved_scan_directories.txt`
3. Run inventory only on that exact child path
