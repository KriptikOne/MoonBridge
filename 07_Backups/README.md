# Backups

Timestamped snapshots of MoonBridge records and indexes.

Use `05_Helper_Tools/backup_snapshot.sh` after reviewing it. Default is dry-run.

Snapshots are copy-only — nothing here is deleted automatically.
